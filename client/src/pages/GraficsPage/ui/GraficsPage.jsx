import React from 'react';
import { classNames } from '../../../shared/lib/classNames/classNames'
import cls from './GraficsPage.module.scss';

const GraficsPage = () => {
    return (
        <div className={classNames(cls.GraficsPage, {}, [])}>
            
        </div>
    );
};

export default GraficsPage;