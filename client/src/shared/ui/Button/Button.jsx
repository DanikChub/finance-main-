import React from 'react';

const Button = (props) => {
    const {
        children,
        className,
        ...otherProps
    } = props;
    return (
        <button>
            
        </button>
    );
};

export default Button;